Features:
More than 1 constuctor
Support for pointer|link types
C++ templates support
Custom types definition
Custom operators 
Typecasts to|from custom types
32-bit systems support

Lib:
state .add (type|value|function) 

Common type for SGL::type* ?
make lib header only?

add move&copy constuctors and assignment operators for value & value type

how differ:
get<std::vector<int>>()//vector<int> is value (T = std::vector<int>)
and
get<std::vector<int>>() //array of int (T = int[])