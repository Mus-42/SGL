Features:
auto kywd
operators for pointers and other non-ref types? 
type for type (result of typeof)?
type aliases

Lib:
check 32-bit systems support
cmake install
what value get<arr<T>*> shold return? pointer to array_impl?
implement type functions for array
replace "//protected:" with macro in config.hpp?

fix multiple clones of same type (get value_type ptr from state?)

replace asserts like bad value cast, illegal name etc. with exceptions

fast function overload search (now it O(nlog(n)))

replace 'SGL' in namespaces & files with lowercase? ('sgl')
