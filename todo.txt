Lang:
add top-level array & dynamic array support
custom types definition
ternary operator (a ? b : c)
basic math functions support
enumerations

Lib:
function call impl

do somesing with C++ exceptions in C API?
add parse_... functions for string, ...? (in C++)
add CMake Install for library?
for C API add file parsing for FILE*
add redefine check
add config.hpp?
replase get_local_vaue(res, name, void* data) in c void* get_local_vaue(res, name)?
add get type from state? custom types definition -> get type from parse_result??
add error levels? (warning, fatal ... ?)
add tests
set minimum type size = 1 byte (for allocations)
add default error function (print errors in std::cerr?)
add init function for library??

Examples:
add more SGL (*.sgl files) for examples?